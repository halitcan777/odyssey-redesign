# Дизайн-правила — редизайн ГК «Одиссей»

Обязательны для всех страниц проекта. Отступления — только по прямому указанию заказчика.
Правка в одном блоке имплементируется во все блоки той же логики построения и смысла.

## 1. Палитра (тёмная, акценты в тему логотипа)

| Токен | Значение | Применение |
|---|---|---|
| `--bg` | `#101114` | фон страницы |
| `--cell` | `#16181C` | фон ячейки бенто |
| `--cell-hover` | `#1C1F24` | ячейка при hover / приподнятая поверхность |
| `--line` | `#26282E` | ВСЕ границы, 1px, общие между ячейками |
| `--ink` | `#F2F1EE` | основной текст |
| `--muted` | `#9BA0A8` | вторичный текст |
| `--red` | `#C8102E` | акцент №1: кнопки, киккеры, цифры, номера (из щита логотипа) |
| `--red-hover` | `#E01236` | hover красной кнопки |
| `--gold` | `#C9A227` | акцент №2, дозированно: метки лицензий, мелкие маркеры (из линии логотипа) |

- На красном фоне текст всегда белый `#FFFFFF`.
- Золото никогда не используется для кнопок и крупных заливок.
- Фотографии: `filter: grayscale(.85) contrast(1.05)`, цвет возвращается при hover (`grayscale(0)`, transition 400ms) — единственная «цветная» награда в интерфейсе.

## 2. Скругления и границы

- `border-radius: 0` ВЕЗДЕ: блоки, кнопки, инпуты, фото, таблицы, плашки.
- Границы только `1px solid var(--line)`.
- Бенто с общими границами реализуется единственным способом:
  `display:grid; gap:1px; background:var(--line); border:1px solid var(--line)` — ячейки с фоном `--cell`, линия сетки = общая граница. Никаких двойных бордеров и margin-хаков.
- Тени запрещены. Глубина — только цветом фона ячейки.

## 3. Система отступов (шкала 8, внешний > внутреннего)

| Шаг | Применение |
|---|---|
| 4 | внутри составного лейбла (иконка-текст) |
| 8 | киккер → заголовок ячейки |
| 12 | заголовок → текст ячейки |
| 16 | текст → второстепенный ряд; зазор между пунктами списков |
| 24 | паддинг компактной ячейки (стат, контакт) |
| 32 | паддинг стандартной ячейки |
| 48 | паддинг крупной ячейки (герой, секционные заголовки) |
| 64 | вертикальный паддинг мега-ячейки героя |

- Внутри одного бенто-полотна отступов между ячейками НЕТ — только общая 1px линия.
- **Действующий вариант (по решению заказчика 24.07): МОНОЛИТ** — вся страница от статов до футера одно непрерывное полотно, стыки секций через `bento--seamtop`, воздух остаётся только в герое. Внутренние паддинги ячеек (24/32/48) не меняются.
- Вариант с полотнами и отступом 96px сохранён в git-теге `v1-polotna-96px` (откат: `git checkout v1-polotna-96px -- index.html`).
- Заголовок всегда ближе к своему контенту, чем к соседнему блоку (закон близости).
- Контейнер: `max-width: 1280px`, боковые поля 24px. На всю ширину уходят только тикер и футер.

## 4. Текстовая иерархия

| Уровень | Шрифт | Кегль/межстрочный | Регистр | Применение |
|---|---|---|---|---|
| H1 | Oswald 700 | clamp(44–64) / 1.02 | UPPERCASE | один на страницу, герой |
| H2 | Oswald 700 | 36 / 1.05 | UPPERCASE | заголовок секции-ячейки |
| H3 | Oswald 600 | 19 / 1.15 | UPPERCASE | заголовок ячейки |
| Цифра-стат | Oswald 700 | 44–56 / 1 | — | счётчики, цены-хедлайны |
| Body | Inter 400 | 15 / 1.6 | — | основной текст |
| Small | Inter 400 | 13 / 1.5 | — | подписи в ячейках |
| Caption/киккер | Inter 600 | 11 / 1.4, letter-spacing .2em | UPPERCASE | киккеры (красные), метки, номера |
| Цена | Oswald 700 | 24–30 | — | всегда с ₽, тонкая подпись Inter 12 рядом |

- Пара шрифтов фиксирована: Oswald (display) + Inter (body). Третий шрифт запрещён.
- Номер ячейки: `01 /` Inter 600 11px, красный, letter-spacing .18em.

## 5. Паттерны построения блоков

- **Ячейка бенто**: киккер/номер сверху → H3 → текст → прижатый к низу ряд (цена, ссылка, тег). Внутренний паддинг 32; flex-column, `margin-top:auto` у нижнего ряда.
- **Фото-ячейка**: фото заполняет ячейку целиком (`object-fit:cover`), ч/б по умолчанию (см. §1). Подпись — плашка `--cell` с 1px границей в углу поверх фото, отступ 0 (примыкает к границам ячейки).
- **Стат-ячейка**: цифра Oswald сверху, подпись `--muted` 13px снизу, паддинг 24.
- **Таблица**: строки = ячейки бенто (grid, gap:1px), шапка — фон `--cell-hover`, uppercase caption.
- **Герой**: сетка 7/5 из 12 колонок; слева текст-мега-ячейка, справа фото-ячейка с выносками-плашками (фон `--cell`, 1px граница, красная точка-указка 8px + линия 1.5px).
- **Плейсхолдер** (нет материала): пунктирная граница `1px dashed #3A3E46`, диагональная штриховка фона, по центру uppercase caption: `НУЖЕН МАТЕРИАЛ: <что именно>`. Блок собирается по сетке как настоящий — заменяется материалом без перевёрстки.
- **Грид-защита**: у текстовых элементов внутри grid-строк принудительно `min-width:0` (иначе min-content распирает трек и ломает мобильную ширину). `grid-column:span N` — только через класс `.spanN` со сбросом в мобильных брейкпоинтах, не инлайном.

## 6. Взаимодействия

- Hover ячейки: фон `--cell` → `--cell-hover`, transition 200ms. Без transform, без теней, без bounce.
- Hover кнопки: primary `--red`→`--red-hover`; secondary (outline 1px `--ink`) → заливка `--ink`, текст `--bg`. Без смещений.
- Кнопки: прямоугольник, Inter 600 13px uppercase, letter-spacing .08em, паддинг 16×28.
- Reveal при скролле: только fade (opacity 0→1, 500ms). Слайды и масштабирование запрещены.
- Счётчики цифр: анимация 1200ms при входе в вьюпорт, easeInOut, один раз.
- Тикер (полоса техники): красная полоса на всю ширину, Oswald 600 uppercase, линейный бесконечный ход 34s. Единственная постоянная анимация на странице.
- Прогресс скролла: линия 3px `--red` по верхней кромке.
- Якорная навигация: `scroll-behavior: smooth`, sticky-шапка с фоном `--bg` (без blur-стекла).
- `prefers-reduced-motion: reduce` — reveal и счётчики отключаются, контент виден сразу.

## 6a. Мобильные правила (390px — обязательная цель)

- Брейкпоинты: 1020 (сетки в 1–2 колонки), 760 (таблицы в стек), 700 (герой), 560 (все сетки в 1 колонку).
- Выноски героя: absolute-плашки на фото → обычные бенто-ячейки 2×2 ПОД фото; пиксель-блобы и линии-указки скрываются.
- Строка-услуга `44px 1fr auto` → `44px 1fr`, тег переезжает под текст (`grid-column:2`).
- Шапка таблицы (`.trow.head`) на мобиле скрывается — строки самоописательны.
- `nav-links` скрываются ≤1020; в шапке остаются лого и телефон (звонок важнее навигации).
- Шрифты подключаются ЛОКАЛЬНО (`fonts/fonts.css`, woff2 cyrillic+latin) — без внешних CDN.
- Проверка ширины: `document.documentElement.scrollWidth === clientWidth` на 390 и 1440 — обязательный замер, скриншоту безголового Chrome не верить (минимальная ширина окна 500px даёт ложный «обрез»; мобильные скрины — только через iframe-обёртку `_mob.html` или реальный браузер).

## 7. Контроль качества (перед каждой сдачей)

1. Скриншот-проверка десктоп 1440 и мобайл 390 — по секциям.
2. Grep-чек: в CSS нет `border-radius` (кроме `0`), нет `box-shadow`, нет второго шрифта.
3. Все цены и факты — только из реальных данных сайта gpodyssey.ru (файл `gpodyssey_texts.json`).
4. Все фото — реальные материалы «Одиссея»; при отсутствии — плейсхолдер по §5.
5. Клик по всем якорям меню, hover-состояния, работа формы-заглушки.

## Запрошенные материалы (ожидают ответа заказчика)

1. Логотип в векторе или PNG ≥1000px (сейчас — реконструкция SVG по скриншоту).
2. Фото прибора полиграфа «РИФ» — для ячейки центра детекции лжи.
3. Фото учебного класса УЦ — для секции обучения.
4. Хайрез фото охранника в экипировке (текущее 800×534, для ретины слабовато).
5. Качественное фото стрелковой галереи изнутри (текущее тёмное, 756×500).
