# Редизайн сайта ГК «Одиссей» (Пермь)

Прототип нового сайта группы предприятий безопасности: охрана, стрелковый тир, учебный центр, полиграф.
**Живая версия:** https://halitcan777.github.io/odyssey-redesign/ · старый сайт: gpodyssey.ru

## Прочитай перед работой

1. **ДИЗАЙН-ПРАВИЛА.md** — палитра, отступы, типографика, бенто, взаимодействия. Отступать от правил можно только по слову заказчика. Правка в одном блоке применяется ко всем блокам той же логики.
2. **ПЛАН-МНОГОСТРАНИЧНИК.md** — структура сайта и порядок работ.
3. Весь контент (цены, тексты, телефоны) — только реальный, со старого сайта. Ничего не выдумываем. Если материала нет — плейсхолдер «НУЖЕН МАТЕРИАЛ: …» (правила §5).

## Структура репо

| Файл/папка | Что это |
|---|---|
| `index.html` | главная-хаб (правится руками) |
| `tir, muzey, uc, ohrana, poligraf, dela, o-nas .html` | внутренние страницы — правятся руками |
| `build_pages.py` + `pages_content.py` | ⛔ ЗАМОРОЖЕННЫЙ генератор (не запускать — перезапишет ручные правки) |
| `typograf.py` | прогон текстов через Типограф Лебедева |
| `styles.css` | весь CSS, общий для всех страниц |
| `fonts/` | локальные Oswald+Inter (woff2), без CDN |
| `фото/` | подготовленные фото + `карта_фото.json` (путь старой страницы → локальный файл) |
| `исходники_сайта/` | спарсенный старый сайт: тексты 114 страниц, фотоархив 234 шт, экстрактор (в гит не попадает) |

## ⚠️ Регламент совместной работы (мы + кореша, чтобы ничего не слетало)

1. **Перед началом работы — всегда** `git pull --rebase origin main`. Перед уходом — закоммитить и запушить.
2. **Страницы правятся РУКАМИ в HTML.** Генератор `build_pages.py` ЗАМОРОЖЕН — его запуск перезапишет все ручные правки.
3. Новые тексты прогонять: `python3 typograf.py <файл.html>` (безопасен для разметки).
4. Стили — только в `styles.css` и только через токены (`var(--ink)` и т.д., темы!). **Никаких инлайновых margin/размеров** — инлайн перебивает мобильные медиа-правила (уже ловили баг).
5. После пуша Pages НЕ пересобирается сам: `gh api repos/halitcan777/odyssey-redesign/pages/builds -X POST`.
6. Проверка после правок: обе темы + scrollWidth на 390 и 1440 (см. §7 дизайн-правил).
7. Конфликт в файле — не решать «выбрать свою версию», а совместить правки; спорное — в чат.

## Конвейер правок

```
git pull --rebase origin main
правка HTML руками → python3 typograf.py <файл.html>
# QA: обе темы, scrollWidth==viewport на 390 и 1440, нет border-radius/box-shadow
git commit + push
gh api repos/halitcan777/odyssey-redesign/pages/builds -X POST   # Pages руками!
```

## Грабли, о которые уже спотыкались

- **Безголовый Chrome врёт про мобилу**: минимальная ширина окна 500px → «обрезанные» скрины при --window-size=390. Мобильные скрины только через `_mob.html` (iframe 390px) или реальный браузер.
- Типограф вставляет `<nobr>` в телефоны — `typograf.py` уже вычищает.
- Проверять выкладку по СОДЕРЖИМОМУ (curl + уникальная строка новой версии), а не по классам, которые есть в обеих версиях.
- В грид-строках текстовым элементам нужен `min-width:0`, иначе min-content распирает мобильную ширину.
- `grid-column:span N` — только классом `.spanN` со сбросом в мобильных брейкпоинтах.

## Журнал (лог проекта)

- **24.07** — карта старого сайта (114 стр., sitemap нет), полный парсинг текстов и фото. Прототип: тёмный бенто (графит + красный из логотипа), герой «охранник с выносками» по референсу KRYDEX, реальные цены. Дизайн-правила зафиксированы. Мобильная вёрстка: выноски → бенто 2×2 под фото, единая левая кромка 20px. Пробовали монолит одним полотном — откатили (тег `v2-monolith`), действующий вариант — полотна с 96px (тег `v1-polotna-96px`). Форма заявки перенесена влево. Типографика по Лебедеву (106 nbsp). Многостраничник: тир (арсенал 26 стволов), музей (11 машин), УЦ (8 программ), охрана (прайс пультовой), полиграф, добрые дела (20 экскурсий). Шрифты локализованы (woff2), без внешних CDN.

- **25.07** (кореша) — светлая тема по умолчанию + тумблер тёмной, full-bleed контейнер, живой логотип, страница «О нас», 29 грамот с тумбами, нормализованные фото оружия, анимации героя.
- **26.07** — восстановлен контент со старого сайта: составы подарочных карт (50/70/100 выстрелов, срок год) + правила сертификата, полные правила тира, лицензия мастерской № 14806-ПО, два видео (тир, Одиссей-СБ), FAQ полиграфа с ТК РФ, музей обмундирования + 40 образцов, прайс пультовой 11/11, руководство с телефонами. Фикс мобильной шапки (тумблер+лого не влезали в 390) и инлайновых margin. Генератор заморожен, регламент совместной работы. На старом сайте обнаружена спам-инъекция (казино) — DLE взломан.

## Что ждём от заказчика (плейсхолдеры в макетах)

Фото прибора «РИФ», фото класса УЦ, лого в векторе, хайрезы охранника/тира. Полный список — в docx-брифе.
